/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['dummyjson.com', 'cdn.dummyjson.com'], // Allow images from these domains
    },
  };

export default nextConfig;
